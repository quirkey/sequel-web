%table.dataset.qtable
  %thead
    %tr
      - @rows.columns.each do |column|
        %th=column
  %tbody
    - @rows.each do |row|
      %tr.data_item
        - @rows.columns.each do |column|
          %td=row[column]

=will_paginate(@rows)